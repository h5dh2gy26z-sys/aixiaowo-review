# 综述写作系统（Survey/Tutorial）项目构想与功能总览

本文档用于把当前仓库中的两套系统、已有能力、以及“面向计算机领域综述写作（Survey/Tutorial）”的完整构想落到一份中文说明里，便于后续新线程/新阶段快速接续开发。

## 0. 工作区结构（已完成拆分）

当前工作区根目录：`E:\aixiaowo\review`

- `wenxian/`：既有的“文献检索策略 + 批量摘要筛选 + 全文队列 + 报告生成”系统（可复用能力在这里）
- `review_agent/`：新建的“综述写作（Survey/Tutorial）”系统（先 file-first，再考虑接 UI）
- `project_state.md` / `project_session_log.md`：交接摘要与会话日志（跨线程 handoff）

根目录 README：`README.md`（描述两套系统的定位与入口）

## 1. 总目标（我们要解决什么问题）

面向计算机/工科类综述（Survey/Tutorial），构建一个“人机协同”的写作系统，目标是：

1. 让用户从一个研究方向出发，快速得到可用的综述骨架（taxonomy + outline）
2. 在摘要级信息不足的情况下，仍能稳定地产出“表/图计划”和“全文优先级清单”，指导下一步收集哪些全文、抽取哪些字段
3. 对全文材料进行证据级抽取（paper cards），再生成可追溯的比较表格、分类表格与章节草稿
4. 通过可测 rubric 做质量门禁，避免“写着写着跑偏/幻觉/表格字段不一致/章节缺关键要素”

核心思想：**不是一次性让大模型写完综述**，而是把综述写作拆成可重跑、可回滚、可人工介入的阶段，每阶段有固定输入/输出工件（artifacts）。

## 2. 两套系统的分工（复用与边界）

### 2.1 `wenxian/`（现有系统）应承担的职责

这套系统更擅长“文献工作流上游”：

- 方向 → 检索策略：生成 Scopus/WoS/PubMed/CNKI 的检索式草案
- 批量导入/合并/去重：支持 `.bib` / `.ris` / `.enw` / PubMed `.txt` 等
- 批量摘要筛选：LLM 批处理 include/exclude/uncertain + 原因
- 人工复核：单篇/批量覆盖筛选结果
- 全文队列：对纳入文献维护 full-text 状态（pending/ready/unavailable/deferred 等）
- 报告生成：基于筛选结果/全文就绪结果生成报告（现有“formal_report_module”模式可复用）
- 数据持久化与任务链：以 thread/project/task/dataset 为主线

一句话：`wenxian/` 是“筛选与管理系统”，负责把“候选池”变成“可进入写作阶段的可控语料库”。

### 2.2 `review_agent/`（新系统）应承担的职责

这套系统更擅长“综述写作下游”，并以 file-first 的 artifacts 为核心：

- 摘要级：taxonomy + paper-to-taxonomy 映射 + 表/图计划 + 全文优先级清单
- 全文级：paper cards（结构化证据抽取）+ 表格行数据（符合 schema）+ 章节草稿
- 质量门禁：rubric +（后续）证据可追溯性检查（claims → evidence pointers）

一句话：`review_agent/` 是“写作系统”，负责把“可控语料”变成“可提交的综述草稿与表图”。

### 2.3 两套系统怎么联动（接口原则）

不做“代码层强耦合”作为起点，而是用 artifacts 作为接口：

- `wenxian/` 输出：筛选输出目录（含 `deduped_records.json`、`screening_decisions.json` 等）
- `review_agent/` 通过桥接脚本导入为综述项目工件：
  - `review_agent/review_writing_baseline/tools/import_from_screening_output.py`

后续如果要接 UI，再考虑把 review-writing 作为 `wenxian/` 的一个新 stage/task kind。

## 3. 综述写作系统的“工件驱动”工作流（核心）

### 3.1 为什么需要工件驱动

因为综述写作存在典型难点：

- 摘要信息不足：无法直接填实“方法对比表/数据集表/指标表”
- 需要反复迭代：分类会调整、代表文献会更换、表格字段会扩展
- 需要人介入：taxonomy、章节结构、重点维度往往需要人工定调
- 需要可追溯：结论/表格单元格不能凭空编造，要能回指到 paper_id（后续到页码/段落/chunk）

因此正确的做法是：

- 摘要阶段只做“规划层”（taxonomy + 表/图计划 + 全文优先级）
- 全文阶段再做“证据层”（paper cards + 表格行 + 成文）

### 3.2 综述项目目录（每个 topic 一个 project）

每个综述项目位于：

`review_agent/review_projects/<project_slug>/`

工件合同（强烈建议先读）：  
`review_agent/review_writing_baseline/specs/artifact_contract_survey.md`

阶段操作手册：  
`review_agent/review_writing_baseline/specs/stage_playbook_survey.md`

### 3.3 阶段与工件（按“可重跑”设计）

下面是 Survey/Tutorial 的推荐阶段（每步只做一件事）：

**Stage 00 方向定标**

- 写：`00_project.yaml`
- 关键字段：`audience`、`scope_in`、`scope_out`
- 作用：锁死边界，防止后面漂移

**Stage 01 检索式**

- 写：`01_search_terms.yaml`
- 作用：可编辑检索式草案（不是最终真理）

**Stage 02 规范化导入**

- 写：`02_import/papers_canonical.csv`
- 作用：统一 schema + 稳定 `paper_id`（后续所有引用的主键）

**Stage 03 摘要筛选**

- 写：`03_screening/abstract_decisions.csv`
- 可选：`03_screening/screening_summary.json`（缺失字段统计、年份/期刊分布等）
- 作用：得到 included/uncertain 集合，为 Stage 04 做准备

**Stage 04 规划层（决定你能不能写出“像样综述”的关键）**

Stage 04 不应该只输出 taxonomy，而要输出三件“指挥后续 05/06 的规划工件”：

- 必选：`04_taxonomy/taxonomy.yaml`（abstract-based v0.1）
- 强烈建议（rubric warn）：
  - `04_taxonomy/paper_classification.csv`：每篇 included 映射到 taxonomy 节点（含置信度与理由）
  - `04_taxonomy/fulltext_priority.csv`：每个类别挑代表文献并写清“为何优先 + 需要抽取哪些字段”
  - `04_taxonomy/table_figure_plan.yaml`：把要做的表/图先“计划出来”，并标注哪些字段需要全文

这一步的目标是把“摘要堆”变成：

- 可写的综述骨架（taxonomy）
- 可执行的 next actions（优先下载哪些全文、抽取哪些字段）
- 可落地的产出清单（要做哪些表/图、依赖哪些证据字段）

**Stage 05 全文抽取（证据层）**

- 写：`05_evidence/paper_cards/<paper_id>.json`
- 表格行数据：`05_evidence/extracted_tables/*`
- 未来（RAG）可选：`05_evidence/chunks/chunks.jsonl`（带 page/chunk 指针）

关键原则：Stage 05 的全文抽取应由 `fulltext_priority.csv` + `table_figure_plan.yaml` 驱动，避免“随缘挑几篇导致表格字段缺失/对比维度不齐”。

**Stage 06 写作与表格/图**

- 先写：`06_draft/00_outline.md`
- 再写：`06_draft/*.md`（intro/taxonomy/comparison/challenges/future/…）
- 可选：`06_draft/claims_map.csv`（草稿 claim → evidence pointers，用于可追溯性）

强建议策略：**tables-first drafting**  
先把对比表/分类表做出来，再写长叙述，能显著提升综述质量与一致性。

**Stage 07 质量门禁（rubric）**

- rubic：`review_agent/review_writing_baseline/baseline/rubric_survey.yaml`
- checker：`review_agent/review_writing_baseline/tools/rubric_check.py`
- 作用：把“进度”与“质量缺口”变成可测结果（缺哪个工件、缺哪些建议项）

## 4. 表/图到底怎么做（解决“摘要信息不足”的不稳定问题）

关键做法：**表/图分两阶段**

### 4.1 摘要阶段只做“表/图计划”（不填实）

在 Stage 04 输出 `table_figure_plan.yaml`，里面列：

- 打算做哪些表（以及对应 schema）
- 每张表需要哪些字段（abstract-only 能拿到什么；needs-fulltext 需要抽什么）
- 每张表由哪些 taxonomy 节点/代表文献支撑

这能保证“结构稳定”，即使后面换了代表文献也不会推倒重来。

### 4.2 全文阶段再填实“对比表/指标表/复现性表”

把 Stage 05 的 `paper_cards` 当作“证据仓库”，从 cards 生成表格行数据（schema 固定）：

- `method_comparison`（方法对比）
- `dataset_benchmark`（数据集/基准）
- `decision_matrix`（场景约束 → 推荐方法）
- `taxonomy_overview`（taxonomy 总览表）

对应 schema 在：  
`review_agent/review_writing_baseline/baseline/table_schemas/`

## 5. RAG / 证据定位（后续增强方向）

为什么要做：

- 解决“引用/证据可追溯”
- 支持“从证据片段生成段落”而不是凭记忆写

推荐落地方式（以 artifacts 为中心）：

1. PDF → chunk（带 page 指针）写入 `05_evidence/chunks/chunks.jsonl`
2. 生成向量索引（放在 gitignore 下，如 `review_agent/review_projects/**/vector_index/`）
3. 抽取时让 LLM 输出 evidence pointers（paper_id + page + chunk_id），写入 paper_cards 的 `evidence_pointers`
4. 生成 `claims_map.csv` / `traceability_report.md`：列出“草稿中缺证据的 claim/表格单元格”

## 6. 图式编排（LangGraph 类）的使用方式（后续增强方向）

不把编排框架当“写作能力”，而当“调度能力”：

- 每个 node 对应一个 Stage 工件转换（读 A 写 B）
- 支持 checkpoint、中断/恢复、人机协同（taxonomy/表计划人工改完再继续）

建议先把 Phase 1/2 的 file-first 工具做出来，再决定是否引入图式编排。

## 7. 当前已实现的内容（截至 2026-04-12）

### 7.1 Review Agent 基线（已具备）

位置：`review_agent/review_writing_baseline/`

已具备：

- 权威资料清单与蒸馏笔记：`sources/manifest.yaml` + `sources/notes/`
- 工件合同 + 跨平台规范：`specs/`
- rubric + 表格 schema + 大纲模板：`baseline/`
- prompts（系统与任务块）：`prompts/`
- 工具：
  - `tools/scaffold_project.py`：新建综述项目骨架
  - `tools/rubric_check.py`：rubric 门禁
  - `tools/import_from_screening_output.py`：从 Wenxian 筛选输出导入综述项目工件

并补强了 Stage 04 的规划工件：

- `paper_classification.csv`、`fulltext_priority.csv`、`table_figure_plan.yaml`（模板与 rubric warn）

### 7.2 Wenxian 系统（可复用）

位置：`wenxian/`

可复用模块包括：

- 策略生成（检索式 + 纳排标准）
- 摘要筛选 pipeline（批处理、去重、继续筛选、导出 included/excluded）
- 全文队列与状态管理
- detached module 模式（formal_report_module）可作为 review_agent 后续“阶段任务”的实现参考

## 8. 下一步正式开发（建议从哪里开始）

建议从“最能提升产出质量”的 **Phase 1** 开始（已在 `review_agent/PROJECT_PLAN.md` 写清）：

> **Phase 1：abstracts → taxonomy + mapping + fulltext priority + table/figure plan**

落地目标：

- 给你一批 abstracts（Stage 03 的 included），系统能产出 Stage 04 的四件工件（taxonomy + classification + priority + table/figure plan）
- 你人工微调后，就能稳定驱动全文下载与抽取（Stage 05），并进入 tables-first drafting（Stage 06）

Phase 1 做完后再做：

- Phase 2：full-text → paper cards
- Phase 3：tables-first drafting
- Phase 4：RAG evidence pointers + traceability

## 9. 相关关键文档入口（建议新线程先读）

- 总体路线图：`review_agent/PROJECT_PLAN.md`
- 工件合同：`review_agent/review_writing_baseline/specs/artifact_contract_survey.md`
- 阶段手册：`review_agent/review_writing_baseline/specs/stage_playbook_survey.md`
- Rubric：`review_agent/review_writing_baseline/baseline/rubric_survey.yaml`
- 设计说明：`review_agent/review_writing_baseline/specs/system_design_survey.md`
